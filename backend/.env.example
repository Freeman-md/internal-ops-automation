# Run Playwright in headed mode (true | false)
HEADED=false

# Number of workflow retries before failing
WORKFLOW_RETRIES=1

# Slow down Playwright actions (milliseconds)
# Either of these can be used
SLOWMO=0
PLAYWRIGHT_SLOWMO=0

AI_PROVIDER=openai
OPENAI_MODEL=gpt-4o-mini
OPENAI_API_KEY=
